# CLAUDE.md

이 저장소는 개인 AI 작업 공간이다.
Obsidian Vault이자 Claude Code의 작업 디렉토리로 사용한다.

이 Workspace는 개인 Knowledge, Skills, Wiki, Rules와 실제 개발 프로젝트를 연결하여 Claude가 사용자의 맥락을 이해하고 작업할 수 있도록 하는 것을 목적으로 한다.

자세한 디렉토리 구조와 각 영역의 목적은 `README.md`를 따른다.

## 폴더 역할

| 경로                      | 역할                                                                                                           |
| ----------------------- | ------------------------------------------------------------------------------------------------------------ |
| `.claude/skills/`       | 반복 작업의 절차·규칙·출력 형식. `lecture-summary`, `velog-post`, `notion-organize`, `technical-research` 등의 Skill이 위치한다. |
| `knowledge/<기술>/`       | 사용자가 학습하거나 리서치한 기술 지식. 이후 다른 작업에서 재사용할 수 있도록 정리한다.                                                           |
| `wiki/mistakes/`        | AI 작업 중 발생한 실수와 재발 방지 규칙                                                                                     |
| `wiki/troubleshooting/` | 문제 해결 과정과 원인·해결 방법                                                                                           |
| `wiki/notes/`           | 그 외 장기적으로 참고할 가치가 있는 작업 메모                                                                                   |
| `references/`           | 반복해서 참고할 원본 자료. `conference/`에는 행사별 녹음 원본과 정리본을 저장한다.                                                        |
| `rules/`                | 여러 작업에 공통으로 적용되는 AI 작업 규칙                                                                                    |
| `attachments/`          | Obsidian 노트에 삽입하는 이미지                                                                                        |
| `projects/`             | 실제 개발 프로젝트에 대한 Symbolic Link를 모아두는 디렉토리                                                                      |

`.obsidian/`, `.claudian/`은 Obsidian 및 관련 도구의 설정이므로 임의로 수정하지 않는다.

## 작업 방식

### 1. 관련 컨텍스트를 먼저 확인한다

작업을 시작하기 전에 현재 작업과 관련된 컨텍스트를 탐색한다.

우선순위는 다음과 같다.

1. 관련 `SKILL.md`
2. 관련 `knowledge/<기술>/`
3. 관련 `rules/`
4. 관련 `wiki/mistakes/`
5. 관련 `wiki/troubleshooting/`
6. 대상 프로젝트의 `AGENTS.md`, `README.md` 및 프로젝트 내부 문서

모든 파일을 무작정 읽지 말고 현재 작업과 관련된 내용만 탐색한다.

### 2. Knowledge를 기존 이해의 출발점으로 사용한다

`knowledge/`에는 사용자가 직접 학습하고 정리한 내용이 포함되어 있다.

관련 Knowledge가 있다면 이를 먼저 참고하여 사용자의 기존 이해와 현재 작업을 연결한다.

단, Knowledge의 내용이 항상 최신이거나 정확하다고 가정하지 않는다.
공식 문서나 실제 코드와 충돌하는 경우 차이를 확인하고 설명한다.

### 3. 프로젝트 작업 시 프로젝트 컨텍스트를 우선한다

`projects/` 아래의 프로젝트는 실제 로컬 프로젝트에 대한 Symbolic Link다.

프로젝트에서 작업할 때는 해당 프로젝트의:

- `AGENTS.md`
- `README.md`
- 프로젝트 내부 `CLAUDE.md`
- 기타 프로젝트별 규칙

을 확인한다.

프로젝트별 규칙과 Workspace의 일반적인 규칙이 충돌하는 경우 프로젝트의 구체적인 규칙을 우선한다.

프로젝트 코드는 해당 프로젝트의 Git 저장소에서 관리하며, 이 Workspace 저장소에서는 프로젝트 파일을 추적하거나 커밋하지 않는다.

### 4. 기존 구조를 먼저 이해한다

프로젝트의 코드를 수정하기 전에 관련 코드와 기존 아키텍처를 먼저 확인한다.

기존 프로젝트의 패턴과 컨벤션을 존중하며, 단순한 문제를 해결하기 위해 불필요한 추상화나 구조 변경을 추가하지 않는다.

### 5. 확인하지 않은 내용을 추측하지 않는다

프로젝트 구조, 라이브러리 동작, API 동작 및 요구사항을 임의로 추측하지 않는다.

확인이 필요한 경우 실제 파일, 코드 또는 공식 문서를 확인한 후 판단한다.

## Knowledge 및 Wiki 관리

사용자가 AI의 실수를 지적하거나 새로운 문제를 해결한 경우, 해당 경험을 장기적으로 재사용할 가치가 있는지 판단한다.

필요하다면 다음 위치에 기록하는 것을 제안한다.

- 반복 가능한 실수 → `wiki/mistakes/`
- 문제 해결 과정 → `wiki/troubleshooting/`
- 장기적으로 참고할 개념 → `knowledge/`
- 단순 작업 메모 → `wiki/notes/`

기존 문서와 중복되는 내용이 있는지 먼저 확인한다.

기존 Knowledge나 Wiki를 임의로 삭제하거나 크게 재구성하지 않는다.

## Skill 사용

작업이 특정 Skill에 해당한다면 관련 `SKILL.md`를 먼저 확인하고 그 지침을 따른다.

예:

- 강의/발표 정리 → `lecture-summary`
- Velog 글 작성 → `velog-post`
- Notion 정리 → `notion-organize`
- 기술 조사 → `technical-research`

해당 Skill이 존재하지 않는 경우 기존 결과물의 형식을 참고할 수 있다.

Skill의 지침과 현재 사용자의 요청이 충돌하는 경우 사용자의 명시적인 요청을 우선한다.

## 노트 작성 규칙

- 기본 언어는 한국어로 작성한다.
- 코드, 식별자, 고유명사는 원문을 유지한다.
- Obsidian 문법을 사용한다.
- 노트 간 연결은 `[[노트 이름]]`을 사용한다.
- 이미지는 `![[파일명.png]]`으로 삽입한다.
- 이미지는 `attachments/`에 저장하며 노트 내부에 별도의 이미지 복사본을 만들지 않는다.
- 녹음 원본은 정리본과 별도 파일로 관리한다.
- 정리본 상단에서 `[[원본 파일명]]`으로 원본을 연결한다.
- 공식 문서나 발표 자료를 정리할 때는 출처를 남긴다.
- 기존 노트를 삭제하거나 크게 재구성하기 전에는 사용자에게 확인한다.

## Git

- 이 저장소의 기본 브랜치는 `main`이다.
- 커밋 메시지는 한국어로 작성한다.
- 커밋 메시지에는 무엇을 추가·수정·삭제했는지 알 수 있도록 관련 파일 또는 디렉토리를 명시한다.
- 커밋과 Push는 사용자가 명시적으로 요청했을 때만 수행한다.
- 실제 개발 프로젝트의 Git 작업은 각 프로젝트의 저장소에서 수행한다.
- `projects/`의 Symbolic Link 자체를 이 저장소에서 임의로 삭제하거나 변경하지 않는다.

## Communication

- 사용자의 질문에 필요한 수준으로 설명한다.
- 이미 사용자가 알고 있는 개념을 불필요하게 처음부터 반복하지 않는다.
- 중요한 판단이나 구조 변경이 필요한 경우 왜 그런 선택을 하는지 설명한다.
- 확실하지 않은 내용은 추측하지 않고 확인한다.
- 사용자의 기존 Knowledge와 현재 프로젝트의 실제 구현 사이에 차이가 있다면 그 차이를 명확하게 설명한다.
